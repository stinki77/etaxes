import React from 'react';
import { View, Text } from 'react-native';
export default function IncomeSources(){ return <View style={{flex:1,alignItems:'center',justifyContent:'center'}}><Text>Източници (CSV)</Text></View>; }
